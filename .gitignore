# Dependencies
node_modules/
.venv/

# Build output
dist/
src-tauri/target/

# OS / editor junk
.DS_Store
*.log

# Local agent / editor config
.claude/

# Secrets — local env (tokens etc.), never commit
.env
.env.local
.env.*.local

# Personal & runtime data (not source code) — anchored to repo root so they don't
# accidentally match source dirs like src/features/memory/ (Piku's persistence layer).
/piku-vault/
/memory/
/obsedian for piku/
